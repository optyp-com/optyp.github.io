---
const { plans } = Astro.props;
---

<section id="plans" class="py-20 text-center">
  <div class="container mx-auto max-w-5xl px-4">
    <h2 class="text-4xl font-bold mb-8 text-primary-700">Our Plans</h2>
    <p class="max-w-xl mx-auto mb-12 text-gray-600 text-lg">
      Choose the perfect plan to elevate your profile.
    </p>
    <div class="grid md:grid-cols-3 gap-8">
      {plans.map(plan => (
        <div class="p-6 border rounded shadow bg-white" key={plan.name}>
          <h3 class="text-2xl font-bold mb-4 text-primary-700">{plan.name}</h3>
          <p class="mb-4">
            {plan.features &&
             plan.features.length &&
             plan.features.find(f => f.toLowerCase().includes("total")) ? (
              <>
                <span class="line-through text-gray-400 mr-2">
                  {
                    (() => {
                      const totalLine = plan.features.find(f => f.toLowerCase().includes("total"));
                      const rupeesMatch = totalLine.match(/₹[\d,]+/);
                      const dollarsMatch = totalLine.match(/\$[\d.,]+/);
                      return (
                        <>
                          {rupeesMatch && <span>{rupeesMatch[0]} </span>}
                          {dollarsMatch && <span>({dollarsMatch[0]})</span>}
                        </>
                      );
                    })()
                  }
                </span>
                <span class="text-primary-600 font-extrabold text-3xl">
                  {plan.price}
                </span>
              </>
            ) : (
              <span class="text-primary-600 font-extrabold text-3xl">
                {plan.price}
              </span>
            )}
          </p>
          <ul class="list-disc text-left mb-4 px-4 text-gray-700">
            {plan.features
              .filter(f => !f.toLowerCase().startsWith("total"))
              .map(feature => (
                <li key={feature}>{feature}</li>
              ))}
          </ul>

          <!-- Choose Plan button with underline hover -->
          <button
            onclick="openContactForm()"
            class="inline-block bg-primary-600 text-white px-6 py-3 rounded hover:bg-primary-700 transition-colors hover-underline"
          >
            Choose Plan
          </button>
        </div>
      ))}
    </div>
  </div>
</section>
