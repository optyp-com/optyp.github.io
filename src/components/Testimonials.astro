---
const { testimonials } = Astro.props;
import 'swiper/css';
import 'swiper/css/navigation';
import 'swiper/css/pagination';
---

<section id="testimonials" class="relative py-20 lg:py-28 bg-gradient-to-b from-black via-gray-900 to-black overflow-hidden">
  <div class="container mx-auto max-w-6xl px-6 text-center relative z-10">
    
    <!-- Section Heading -->
    <h2 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold mb-4 text-white">
      Hear From Our <span class="text-cyan-400">Clients Worldwide</span>
    </h2>
    <div class="w-24 h-1 bg-gradient-to-r from-cyan-400 to-blue-500 mx-auto mb-10 rounded-full shadow-lg"></div>

    <!-- Intro -->
    <p class="text-gray-300 text-lg mb-6 max-w-4xl mx-auto">
      We have successfully optimized over 
      <span class="font-semibold text-cyan-400">500+ professional profiles</span> 
      across multiple countries, empowering careers globally.
    </p>
    <p class="mb-12 text-base sm:text-lg font-medium text-gray-400 max-w-3xl mx-auto">
      <strong class="text-cyan-300">We value your privacy.</strong><br />
      Your personal and professional details are never disclosed.  
      Confidentiality and trust are the foundation of our service.
    </p>

    <!-- Swiper Carousel -->
    <swiper-container
      class="pb-16"
      space-between="30"
      slides-per-view="1"
      autoplay-delay="4000"
      autoplay-disable-on-interaction="false"
      pagination="true"
      navigation="true"
      loop="true"
      breakpoints='{"768":{"slidesPerView":2},"1024":{"slidesPerView":3}}'
    >
      {testimonials.map(({ quote, profession, location }) => (
        <swiper-slide>
          <blockquote
            class="relative group glass-card p-8 rounded-2xl transition-all duration-500 hover:scale-105"
          >
            <!-- Profile Icon -->
            <div class="mx-auto mb-4 w-16 h-16 rounded-full bg-gradient-to-r from-cyan-400 to-blue-500 flex items-center justify-center shadow-lg group-hover:scale-110 transition duration-500">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-black animate-pulse" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A9.969 9.969 0 0112 15c2.21 0 4.243.72 5.879 1.929M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </div>

            <!-- Quote -->
            <p class="text-gray-200 italic mb-6 text-lg leading-relaxed">
              "{quote}"
            </p>
            <footer class="text-sm font-bold text-cyan-300 uppercase tracking-wide">
              {profession}, {location}
            </footer>
          </blockquote>
        </swiper-slide>
      ))}
    </swiper-container>

    <!-- Navigation Arrows -->
    <div class="swiper-button-prev hidden md:flex absolute left-0 top-1/2 transform -translate-y-1/2 z-10"></div>
    <div class="swiper-button-next hidden md:flex absolute right-0 top-1/2 transform -translate-y-1/2 z-10"></div>
  </div>
</section>

<!-- Swiper Bundle -->
<script>
  import 'swiper/element/bundle';
</script>

<style>
  /* Glass Card */
  .glass-card {
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(12px);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.6);
    transition: all 0.3s ease;
  }
  .glass-card:hover {
    border-color: rgba(0, 255, 204, 0.6);
    box-shadow: 0 0 25px rgba(0, 255, 204, 0.4);
  }

  /* Pagination Dots */
  .swiper-pagination-bullet {
    background: #00ffcc;
    opacity: 0.4;
    transition: all 0.3s;
  }
  .swiper-pagination-bullet-active {
    background: #00ffcc;
    opacity: 1;
    transform: scale(1.3);
    box-shadow: 0 0 10px #00ffcc;
  }

  /* Navigation Arrows */
  .swiper-button-next,
  .swiper-button-prev {
    color: #00ffcc;
    background: transparent;
    transition: transform 0.3s, color 0.3s;
  }
  .swiper-button-next:hover,
  .swiper-button-prev:hover {
    transform: scale(1.2);
    color: #00e0b8;
  }
  .swiper-button-next::after,
  .swiper-button-prev::after {
    font-size: 1.75rem;
    font-weight: bold;
  }
</style>
