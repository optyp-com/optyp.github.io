---
---
<section id="about" class="relative py-24 lg:py-32 overflow-hidden">
  <!-- Decorative Background -->
  <div class="absolute inset-0 -z-10 bg-gradient-to-br from-white to-primary-50 dark:from-dark-900 dark:to-dark-800"></div>

  <div class="container mx-auto px-6 md:px-12 relative z-10">
    <div class="grid md:grid-cols-2 items-center gap-16">
      
      <!-- Illustration -->
      <div class="flex justify-center md:justify-start animate-float">
        <img
          src="/images/about-resume.svg"
          alt="Career Growth Success Illustration"
          class="w-56 sm:w-72 md:w-full max-w-md drop-shadow-xl"
        />
      </div>

      <!-- Content -->
      <div class="animate-fadeInUp space-y-6">
        <div class="bg-white/50 dark:bg-dark-800/60 backdrop-blur-md border border-white/20 rounded-2xl shadow-soft p-8 sm:p-10 hover:shadow-glass transition">
          
          <!-- Heading -->
          <h2 class="font-heading text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 dark:text-white mb-6">
            About <span class="bg-gradient-to-r from-primary-500 to-secondary-500 bg-clip-text text-transparent">OPTYP</span>
          </h2>

          <!-- Paragraphs -->
          <p class="text-lg leading-relaxed text-gray-700 dark:text-gray-300">
            At <span class="font-semibold text-primary-600 dark:text-primary-400">OPTYP</span>, we specialize in optimizing your 
            <span class="highlight">ATS Resume</span>, 
            <span class="highlight">LinkedIn</span>, 
            <span class="highlight">Indeed</span>, 
            <span class="highlight">Naukri</span>, 
            <span class="highlight">GitHub</span>, and 
            <span class="highlight">Portfolio</span> profiles to help you get noticed and hired faster globally.
          </p>
          <p class="text-lg leading-relaxed text-gray-700 dark:text-gray-300">
            Our expert team ensures your professionalism is showcased to attract recruiters effectively.  
            <span class="font-semibold text-primary-600 dark:text-primary-400">Get Set, Get Hired!</span>
          </p>

          <!-- Stats Row -->
          <div class="mt-10 grid grid-cols-2 gap-8 text-center md:text-left">
            <div>
              <p class="text-4xl font-extrabold text-primary-600 dark:text-primary-400 counter" data-target="500">0</p>
              <p class="text-gray-600 dark:text-gray-400 mt-1">Profiles Optimized</p>
            </div>
            <div>
              <p class="text-4xl font-extrabold text-primary-600 dark:text-primary-400 counter" data-target="30">0</p>
              <p class="text-gray-600 dark:text-gray-400 mt-1">Countries Served</p>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Counter Script -->
  <script type="module">
    const counters = document.querySelectorAll('.counter');
    if (counters.length) {
      function animateCount(el, end, duration = 1500) {
        let startTime;
        function step(now) {
          if (!startTime) startTime = now;
          const progress = Math.min((now - startTime) / duration, 1);
          const current = Math.floor(progress * end);
          el.textContent = current.toLocaleString();
          if (progress < 1) requestAnimationFrame(step);
          else el.textContent = end.toLocaleString() + '+';
        }
        requestAnimationFrame(step);
      }

      const runCounters = () => counters.forEach(el => {
        const target = parseInt(el.dataset.target, 10) || 0;
        animateCount(el, target, 1200 + Math.min(target, 1000));
      });

      const section = document.getElementById('about');
      if ('IntersectionObserver' in window) {
        const io = new IntersectionObserver((entries, obs) => {
          if (entries.some(e => e.isIntersecting)) {
            runCounters();
            obs.disconnect();
          }
        }, { threshold: 0.35 });
        io.observe(section);
      } else {
        runCounters();
      }
    }
  </script>

  <style>
    /* Floating animation */
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    .animate-float { animation: float 6s ease-in-out infinite; }

    /* Highlight keywords */
    .highlight {
      font-weight: 600;
      position: relative;
      color: inherit;
      transition: color 0.3s ease, text-shadow 0.3s ease;
    }
    .highlight:hover {
      color: #16a34a;
      text-shadow: 0 0 8px rgba(22, 163, 74, 0.4);
    }
  </style>
</section>
