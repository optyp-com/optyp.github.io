---
---
<section
  id="hero"
  class="relative flex items-center justify-center min-h-[90vh] overflow-hidden"
>
  <!-- Background with animated gradient -->
  <div class="absolute inset-0 -z-10">
    <div class="w-full h-full bg-gradient-to-br from-primary-500 via-primary-600 to-primary-700 animate-gradient-x"></div>
  </div>

  <div class="container-custom md:pl-8">
    <div class="flex flex-col items-center text-center md:grid md:grid-cols-2 md:gap-12">
      
      <!-- Text Content -->
      <div 
        aria-label="OPTYP Hero" 
        class="mb-10 md:mb-0 animate-fadeInUp"
      >
        <div class="bg-white/20 backdrop-blur-md border border-white/30 rounded-2xl shadow-lg p-6 sm:p-8 inline-block">
          <h1
            class="text-4xl sm:text-5xl md:text-6xl font-heading font-extrabold leading-tight text-white drop-shadow-md"
          >
            Optimize Your <span class="text-primary-100">Professionalism</span>
          </h1>
          <p class="mt-4 text-lg sm:text-xl text-white/90">
            Get resume, LinkedIn, Naukri, and portfolio optimization that makes you stand out worldwide.
          </p>

          <!-- CTAs -->
          <div class="mt-6 flex flex-col sm:flex-row gap-4 justify-center md:justify-start">
            <a 
              href="#contact" 
              class="px-6 py-3 rounded-lg bg-white text-primary-600 font-semibold shadow hover:shadow-lg hover:scale-105 transition"
            >
              Get Started
            </a>
            <a 
              href="#plans" 
              class="px-6 py-3 rounded-lg border border-white text-white font-semibold hover:bg-white/10 shadow hover:scale-105 transition"
            >
              View Plans
            </a>
          </div>
        </div>
      </div>

      <!-- Right Side (Image or Illustration) -->
      <div class="flex justify-center md:justify-end animate-fadeInUp" style="animation-delay:0.2s">
        <img
          src="/images/illu.svg"
          alt="Career Growth"
          class="w-64 sm:w-80 md:w-full max-w-md drop-shadow-xl"
        />
      </div>
    </div>
  </div>

  <!-- Scroll Down Indicator -->
  <div class="absolute bottom-6 left-1/2 transform -translate-x-1/2 animate-bounce">
    <a href="#about" aria-label="Scroll Down">
      <svg class="w-6 h-6 text-white opacity-80" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
      </svg>
    </a>
  </div>

  <!-- Extra Styles -->
  <style>
    /* Animated gradient background */
    @keyframes gradient-x {
      0%, 100% {
        background-position: 0% 50%;
      }
      50% {
        background-position: 100% 50%;
      }
    }
    .animate-gradient-x {
      background-size: 200% 200%;
      animation: gradient-x 10s ease infinite;
    }
  </style>
</section>
