---
---
<section
  id="hero"
  class="relative flex items-center justify-center min-h-[90vh] overflow-hidden"
>
  <div class="absolute inset-0 -z-10">
    <div class="w-full h-full bg-gradient-to-br from-gray-900 via-black to-gray-800 animate-gradient-x"></div>
    <div class="absolute inset-0 bg-black/50"></div>
  </div>

  <div class="container-custom md:pl-8">
    <div class="flex flex-col items-center text-center md:grid md:grid-cols-2 md:gap-12">
      
      <div class="mb-10 md:mb-0">
        <div class="bg-gray-800/30 backdrop-blur-xl border border-white/10 rounded-3xl shadow-xl p-6 sm:p-8 
                    hover:border-green-400/50 hover:shadow-2xl transition-all duration-500">
          <h1 class="animate-fadeInUp [animation-delay:0.1s] text-4xl sm:text-5xl md:text-6xl font-heading font-extrabold leading-tight text-white drop-shadow-lg">
            Optimize Your <span class="text-green-400">Professionalism</span>
          </h1>
          <p class="animate-fadeInUp [animation-delay:0.3s] mt-4 text-lg sm:text-xl text-gray-300/90 max-w-lg mx-auto md:mx-0">
            Get resume, LinkedIn, Naukri, and portfolio optimization that makes you stand out worldwide.
          </p>

          <div class="animate-fadeInUp [animation-delay:0.5s] mt-8 flex flex-col sm:flex-row gap-4 justify-center md:justify-start">
            <a 
              href="#contact" 
              class="px-8 py-3 rounded-xl bg-green-500 text-black font-semibold shadow-lg
                     hover:bg-green-400 hover:scale-105 transition-all duration-300 pulse-cta"
            >
              Get Started
            </a>
            <a 
              href="#plans" 
              class="px-8 py-3 rounded-xl border-2 border-white/60 text-white font-semibold 
                     hover:bg-white/10 hover:scale-105 hover:border-white transition-all duration-300"
            >
              View Plans
            </a>
          </div>
        </div>
      </div>

      <div class="flex justify-center md:justify-end animate-fadeInUp [animation-delay:0.7s] floating">
        <img
          src="/images/illu.svg"
          alt="Career Growth"
          class="w-64 sm:w-80 md:w-full max-w-md drop-shadow-[0_0_35px_rgba(52,211,153,0.3)]"
        />
      </div>
    </div>
  </div>

  <div class="absolute bottom-6 left-1/2 -translate-x-1/2 animate-bounce">
    <a href="#about" aria-label="Scroll Down" class="group">
      <svg class="w-8 h-8 text-white opacity-70 group-hover:text-green-300 group-hover:opacity-100 transition-all drop-shadow-lg" fill="none" stroke="currentColor" viewBox="0 0 24 24">
         <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
      </svg>
    </a>
  </div>

  <style>
    @keyframes gradient-x {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    .animate-gradient-x { background-size: 200% 200%; animation: gradient-x 15s ease infinite; }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-15px); }
    }
    .floating { animation: float 6s ease-in-out infinite; }
    @keyframes fadeInUp {
      0% { opacity: 0; transform: translateY(30px); }
      100% { opacity: 1; transform: translateY(0); }
    }
    .animate-fadeInUp { opacity: 0; animation: fadeInUp 1s ease forwards; }
    @keyframes pulse {
      0%, 100% { transform: scale(1); box-shadow: 0 0 0 rgba(52,211,153,0); }
      50% { transform: scale(1.05); box-shadow: 0 0 25px rgba(52,211,153,0.5); }
    }
    .pulse-cta { animation: pulse 2.5s infinite; }
  </style>
</section>