<section class="bg-gray-100 px-4 py-12 max-w-4xl mx-auto" id="faq">
  <h2 class="text-3xl font-heading font-bold mb-8 text-center">Frequently Asked Questions</h2>
  <ul class="space-y-4">
    <li>
      <button class="faq-question w-full text-left font-semibold text-primary-700" aria-expanded="false" aria-controls="faq1" onClick={() => { 
        const a = document.getElementById('faq1'); 
        const e = event.currentTarget; 
        const expanded = e.getAttribute('aria-expanded') === 'true'; 
        e.setAttribute('aria-expanded', !expanded); 
        a.classList.toggle('hidden'); 
      }}>
        What is included in the Essentials plan?
      </button>
      <p id="faq1" class="faq-answer hidden mt-2 text-gray-700">
        The Essentials plan includes ATS Resume, Naukri Profile, and LinkedIn Optimization services.
      </p>
    </li>
    <li>
      <button class="faq-question w-full text-left font-semibold text-primary-700" aria-expanded="false" aria-controls="faq2" onClick={() => { 
        const a = document.getElementById('faq2'); 
        const e = event.currentTarget; 
        const expanded = e.getAttribute('aria-expanded') === 'true'; 
        e.setAttribute('aria-expanded', !expanded); 
        a.classList.toggle('hidden'); 
      }}>
        How long does delivery take?
      </button>
      <p id="faq2" class="faq-answer hidden mt-2 text-gray-700">Delivery typically occurs within 48 hours after purchase.</p>
    </li>
    <li>
      <button class="faq-question w-full text-left font-semibold text-primary-700" aria-expanded="false" aria-controls="faq3" onClick={() => { 
        const a = document.getElementById('faq3'); 
        const e = event.currentTarget; 
        const expanded = e.getAttribute('aria-expanded') === 'true'; 
        e.setAttribute('aria-expanded', !expanded); 
        a.classList.toggle('hidden'); 
      }}>
        Can I order individual services?
      </button>
      <p id="faq3" class="faq-answer hidden mt-2 text-gray-700">Yes, you can select individual services based on your needs.</p>
    </li>
    <li>
      <button class="faq-question w-full text-left font-semibold text-primary-700" aria-expanded="false" aria-controls="faq4" onClick={() => { 
        const a = document.getElementById('faq4'); 
        const e = event.currentTarget; 
        const expanded = e.getAttribute('aria-expanded') === 'true'; 
        e.setAttribute('aria-expanded', !expanded); 
        a.classList.toggle('hidden'); 
      }}>
        What is your revision policy?
      </button>
      <p id="faq4" class="faq-answer hidden mt-2 text-gray-700">Elite plan includes up to 2 revisions; other plans vary.</p>
    </li>
  </ul>
</section>
