---
import { faqs } from "../data/faqs.js";
import Contact from "./contact.astro";
import FAQSearch from "./FAQSearch.astro";

// Group FAQs by category
const groupedFaqs = faqs.reduce((acc, faq) => {
  if (!acc[faq.category]) acc[faq.category] = [];
  acc[faq.category].push(faq);
  return acc;
}, {});
---

<section class="bg-gray-50 py-20 px-4" id="faq">
  <div class="container mx-auto max-w-5xl">
    <div class="text-center mb-12">
      <h2 class="text-4xl font-extrabold mb-4 text-gray-900">All Frequently Asked Questions</h2>
      <div class="w-20 h-1 bg-gradient-to-r from-primary-500 to-primary-700 mx-auto mb-10 rounded-full"></div>
    </div>

    <!-- Search Bar -->
    <div class="relative max-w-lg mx-auto mb-10">
      <input
        id="faq-search"
        type="text"
        placeholder="🔍 Search FAQs..."
        class="w-full px-5 py-3 rounded-xl border border-gray-300 shadow-sm focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500"
      />
    </div>

    <!-- FAQ Categories -->
    <div id="faq-list" class="space-y-12">
      {Object.entries(groupedFaqs).map(([category, items], catIndex) => (
        <div class="faq-category">
          <h3 class="text-2xl font-bold text-primary-600 mb-6">{category}</h3>
          <div class="space-y-6">
            {items.map((faq, i) => (
              <div class="faq-item group bg-white rounded-lg shadow hover:shadow-lg transition hover:border-primary-500"
                   data-aos="fade-up" data-aos-delay={i * 100}>
                <button class="faq-question w-full text-left p-6 font-bold text-lg text-gray-900 flex justify-between items-center"
                  aria-expanded="false"
                  aria-controls={`faq-${catIndex}-${i}`}
                  onclick={`toggleFAQ('faq-${catIndex}-${i}', this)`}>
                  <span class="faq-text">{faq.question}</span>
                  <svg class="w-6 h-6 transform transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                <div id={`faq-${catIndex}-${i}`} class="faq-answer hidden px-6 pb-6 text-gray-700 text-base">
                  <p class="py-4 faq-text" set:html={faq.answer}></p>
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>

    <!-- No Results -->
    <p id="faq-no-results" class="hidden text-center text-gray-600 mt-8">❌ No FAQs found. Try a different keyword.</p>

    <!-- Contact CTA -->
    <div class="text-center mt-12 p-6 bg-white rounded-lg shadow hover:shadow-lg transition hover:border-primary-500">
      <h3 class="text-xl font-bold text-gray-900 mb-2">Still have questions?</h3>
      <p class="text-gray-700 mb-4">Our team is here to help you succeed in your career journey.</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <button
          id="faq-contact-btn"
          class="px-6 py-3 bg-primary-600 text-white font-semibold rounded-lg shadow hover:bg-primary-700 transition"
          onclick="openContactForm()"
        >
          Contact Us
        </button>
        <a href="https://wa.me/919848934274"
          class="px-6 py-3 border border-primary-600 text-primary-600 font-semibold rounded-lg hover:bg-primary-50 transition"
          target="_blank" rel="noopener">
          WhatsApp Support
        </a>
      </div>
    </div>
  </div>

  <Contact />

  <!-- Toggle Script -->
  <script is:inline>
    function toggleFAQ(faqId, button) {
      const ans = document.getElementById(faqId);
      const icon = button.querySelector('svg');
      const expanded = button.getAttribute('aria-expanded') === 'true';
      document.querySelectorAll('.faq-answer').forEach(el => el.classList.add('hidden'));
      document.querySelectorAll('.faq-question').forEach(b => {
        b.setAttribute('aria-expanded', 'false');
        b.querySelector('svg').style.transform = 'rotate(0deg)';
      });
      if (!expanded) {
        ans.classList.remove('hidden');
        button.setAttribute('aria-expanded', 'true');
        icon.style.transform = 'rotate(180deg)';
      }
    }
  </script>

  <!-- Reusable Search -->
  <FAQSearch inputId="faq-search" listId="faq-list" noResultsId="faq-no-results" />
</section>
