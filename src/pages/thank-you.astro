---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Thank You - OPTYP" description="Your message has been sent successfully. We'll get back to you soon!">
  <section class="min-h-[70vh] flex flex-col items-center justify-center text-center px-4 bg-gradient-to-br from-gray-50 via-gray-100 to-gray-200">
    <div class="max-w-lg mx-auto bg-white/30 backdrop-blur-lg border border-white/40 rounded-2xl shadow-md p-10 
                transform translate-y-10 opacity-0 transition-all duration-700 ease-out"
         id="thankyou-card">
      
      <!-- Success Icon -->
      <div class="flex items-center justify-center w-20 h-20 mx-auto mb-6 rounded-full bg-green-100/70 text-green-600 shadow-inner 
                  animate-pulse-glow">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-12 h-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
      </div>

      <!-- Heading -->
      <h1 class="text-4xl font-extrabold text-gray-900 mb-4">Thank You!</h1>
      <p class="text-lg text-gray-700 mb-8">
        Your message has been sent successfully. Our team will get back to you soon.  
        We’re excited to help you optimize your career journey 🚀
      </p>

      <!-- Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/" 
          class="inline-flex items-center justify-center px-6 py-3 bg-gradient-to-r from-primary-600 to-primary-700 text-white text-lg font-semibold rounded-xl shadow-md hover:shadow-lg hover:scale-105 transition">
          Back to Home
        </a>
        <a href="/#plans" 
          class="inline-flex items-center justify-center px-6 py-3 bg-white/80 text-primary-600 border border-primary-600 text-lg font-semibold rounded-xl shadow-md hover:bg-white hover:scale-105 transition">
          View Our Plans
        </a>
      </div>
    </div>

    <!-- Animation Script -->
    <script is:inline>
      window.addEventListener("DOMContentLoaded", () => {
        const card = document.getElementById("thankyou-card");
        requestAnimationFrame(() => {
          card.classList.remove("translate-y-10", "opacity-0");
          card.classList.add("translate-y-0", "opacity-100");
        });
      });
    </script>

    <!-- Custom Tailwind Animation -->
    <style>
      @keyframes pulseGlow {
        0%, 100% {
          box-shadow: 0 0 0 0 rgba(34,197,94,0.5); /* green-500 */
        }
        50% {
          box-shadow: 0 0 20px 5px rgba(34,197,94,0.3);
        }
      }
      .animate-pulse-glow {
        animation: pulseGlow 2s infinite;
      }
    </style>
  </section>
</Layout>
