# ✅ /netlify.toml
# Configuration for deploying Astro + Netlify Functions

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
